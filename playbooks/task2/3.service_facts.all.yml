---
- name: ping
  hosts: web
  #become: yes
  gather_facts: yes
  tasks:
  - name: Populate service facts
    service_facts:

  - name: Print service facts
    ansible.builtin.debug:
      var: ansible_facts.services
      verbosity: 2
